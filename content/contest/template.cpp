#include "bits/stdc++.h"
using namespace std;
#ifndef DEBUG
#define debug(...)
#endif
#define FASTIO ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define sz(v) (int)(begin(v), end(v))
#define all(v) begin(v), end(v)
#define pb push_back
#define ff first
#define ss second
using ll = long long; using ld = long double; using pii = pair<int,int>; using pll = pair<ll, ll>;
const int INF = 2e9 + 42069; const ll INFLL = (ll)INF * (ll)INF; const ld EPS = 10e-9;
mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
#define rand(l, r) uniform_int_distribution<int>(l, r)(rng)
#define randll(l, r) uniform_int_distribution<ll>(l, r)(rng)
#define shuffle(v) shuffle(all(v), rng)
int32_t main(){
	FASTIO;
	return 0;
}